/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'https://nextjs-dashboard-beta-flax.vercel.app',
        ]
    },
};

module.exports = nextConfig;
